import React from "react";
import { useSelector } from "react-redux";

const TopBar = () => {

  // const totalItems = useSelector(selectCount);
  return (
    <div style={{width: '100%', backgroundColor: 'rgb(58,132,300)', padding: '10px', color: 'white' }}>Your Task Actions - {/*totalItems*/}</div>
  )
}

export default TopBar;
